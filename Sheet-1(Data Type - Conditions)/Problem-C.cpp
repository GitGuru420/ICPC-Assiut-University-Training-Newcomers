#include <bits/stdc++.h>
using namespace std;

int main() {
    long long x, y;
    cin >> x >> y;

    // summation
    cout << x << " + " << y << " = " << x + y << endl;

    // multiplication
    cout << x << " * " << y << " = " << x * y << endl;

    // subtraction
    cout << x << " - " << y << " = " << x - y << endl;

    return 0;
}

// C Code
/*
    #include <stdio.h>
    int main() {
        long long x, y;
        scanf("%lld %lld", &x, &y);

        // summation
        printf("%lld + %lld = %lld\n", x, y, (x+y));

        // multiplication
        printf("%lld * %lld = %lld\n", x, y, (x*y));

        // subtraction
        printf("%lld - %lld = %lld\n", x, y, (x-y));

        return 0;
    }
*/